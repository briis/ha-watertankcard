{
  "name": "water-tank-card",
  "version": "1.0.0",
  "description": "HACS custom Lovelace card for water tank level, volume and distance monitoring",
  "keywords": ["home-assistant", "hacs", "lovelace", "custom-card"],
  "license": "MIT",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c --watch",
    "serve": "serve . -l 3000 --no-clipboard",
    "start": "concurrently --names rollup,serve --prefix-colors cyan,green \"npm run dev\" \"npm run serve\""
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.3.0",
    "@rollup/plugin-terser": "^0.4.4",
    "concurrently": "^9.1.0",
    "rollup": "^4.29.0",
    "serve": "^14.2.4"
  }
}
